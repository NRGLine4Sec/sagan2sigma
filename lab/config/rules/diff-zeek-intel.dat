# Zeek Intel feed for the corpus differential, distinct from lab-zeek-intel.dat.
#
# Five addresses so that every position a rule can declare is an indicator on
# both sides. Tab separated, as src/processors/zeek-intel.c reads it, and the
# Vector side derives its enrichment table from this same file.
203.0.113.21	Intel::ADDR	differential indicator
203.0.113.22	Intel::ADDR	differential indicator
203.0.113.23	Intel::ADDR	differential indicator
203.0.113.24	Intel::ADDR	differential indicator
203.0.113.25	Intel::ADDR	differential indicator
